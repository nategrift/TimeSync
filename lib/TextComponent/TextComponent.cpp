#include "TextComponent.h"
// #include "HD44780.h"
#include "UIManager.h"

void TextComponent::render(int yOffset) {
    // LCD_setCursor(0, yOffset);
    // LCD_writeStr(const_cast<char*>(text.c_str()));
    // ESP_LOGI("TextComponent", "%s", text);
}